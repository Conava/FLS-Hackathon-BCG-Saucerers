# Backend URL — override in .env.local for local dev
BACKEND_URL=http://localhost:8080

# Demo patient IDs — format: email:patient_id (comma-separated for multiple)
DEMO_PATIENT_IDS=rebecca@example.com:pt-0199
